<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/main.css">
</head>
<body class="grey-bg">

    <%- include('nav.ejs') %>
    <!-- 이렇게 하면 nav.ejs 에 있는 내용이 복사됨 -->

    <form class="form-box" action="/edit?_method=PUT" method="POST">
        <h4>수정하기</h4>
        <input name="id" value="<%= posts._id %>" style="display: none">
        <input name="title" value="<%= posts.title %>">
        <input name="content" value="<%= posts.content %>">
        <button type="submit">수정</button>
    </form>
</body>
</html>