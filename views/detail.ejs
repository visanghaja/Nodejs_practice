<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/main.css">
</head>
<body class="grey-bg">

    <%- include('nav.ejs') %>
    <!-- 이렇게 하면 nav.ejs 에 있는 내용이 복사됨 -->

    <div class="detail-bg">
        <h4><%= posts.title %></h4>
        <p><%= posts.content %></p>
        <p>writer : <%= posts.username %></p>
        <img src="<%= posts.img %>">
    </div>

    <!-- query로 ? =  -->
    <div> 
        <form action="/invite" method="POST">
            <button type="submit">초대</button>
            <input name="writer" value="<%= posts.user %>" style = 'display: none'>
        </form>
    </div>

    <div class="comment">
        <form action="/detail/<%= posts._id %>" method="POST">
            <input name="comment" class="search">
            <button type="submit" class="search-send">입력</button>
        </form>

        <div class="white-bg">
            <% for (let i = 0; i < comment.length; i++){ %>
                <div class="list-box">
                    <p><strong><%= comment[i].writer %></strong> : <%= comment[i].comment %></p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>